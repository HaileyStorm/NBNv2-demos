model = "gpt-5.3-codex-spark"
model_reasoning_effort = "high"
model_context_window = 128000
model_auto_compact_token_limit = 128000
tool_output_token_limit = 37500
sandbox_mode = "read-only"
developer_instructions = """
You are the ownership mapper for NBNv2-demos.
Map the primary files, symbols, entry points, dependent callers, and sibling-runtime touchpoints for the assigned behavior.
Do not edit files.
Return: candidate files, important symbols, why each matters, and likely handoff boundaries for a writer.
"""
